[package]
name = "public-edge-client"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]
anyhow = "1"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
